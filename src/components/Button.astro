---
import { cva, type VariantProps } from 'class-variance-authority';

const button = cva(
  // Base styles matching design system .btn class
  'inline-flex items-center justify-center font-sans font-medium transition-all duration-200 ease-out-expo focus:outline-none focus:ring-3 focus:ring-primary focus:ring-offset-2 disabled:opacity-40 disabled:pointer-events-none',
  {
    variants: {
      variant: {
        // Primary: Egyptian gold background with dark text
        primary: 'bg-primary text-accent border-2 border-primary hover:shadow-[0_8px_16px_rgba(244,196,48,0.2)] hover:border-primary-dark hover:scale-[1.02] active:scale-[0.98]',
        // Secondary: Transparent with dark border
        secondary: 'bg-transparent text-accent border-2 border-accent hover:bg-accent hover:text-surface hover:scale-[1.02] active:scale-[0.98]',
      },
      size: {
        sm: 'px-4 py-2 text-sm rounded-md',
        md: 'px-6 py-3 text-base rounded-lg',
        lg: 'px-8 py-4 text-lg rounded-lg',
      },
    },
    defaultVariants: {
      variant: 'primary',
      size: 'md',
    },
  }
);

type Props = VariantProps<typeof button> & {
  href?: string;
  class?: string;
  type?: 'button' | 'submit' | 'reset';
};

const { variant, size, href, class: className, type = 'button' } = Astro.props;
---

{href ? (
  <a href={href} class={button({ variant, size, className })}>
    <slot />
  </a>
) : (
  <button type={type} class={button({ variant, size, className })}>
    <slot />
  </button>
)}
