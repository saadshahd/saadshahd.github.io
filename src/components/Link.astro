---
import { cva, type VariantProps } from 'class-variance-authority';
import { clsx } from 'clsx';

const link = cva(
  'transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2',
  {
    variants: {
      variant: {
        nav: 'text-slate-700 hover:text-primary font-medium focus:ring-primary',
        inline: 'text-secondary border-b border-secondary-light hover:text-accent hover:border-accent focus:ring-secondary',
        external: 'text-secondary hover:text-accent inline-flex items-center gap-1 focus:ring-accent',
      },
      active: {
        true: 'text-primary',
      },
    },
    defaultVariants: {
      variant: 'inline',
    },
  }
);

type Props = VariantProps<typeof link> & {
  href: string;
  class?: string;
  external?: boolean;
};

const { variant, active, href, class: className, external = false } = Astro.props;
const classes = clsx(link({ variant, active }), className);

// External link attributes
const externalAttrs = external ? {
  target: '_blank',
  rel: 'noopener noreferrer'
} : {};
---

<a href={href} class={classes} {...externalAttrs}>
  <slot />
  {external && variant === 'external' && (
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
    </svg>
  )}
</a>
